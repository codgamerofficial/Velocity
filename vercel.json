{
    "buildCommand": "chmod +x node_modules/.bin/vite && node_modules/.bin/vite build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}